---
import Layout from './Layout.astro';
import CloudinaryImage from '../components/CloudinaryImage.astro';
import CloudinaryShowcase from '../components/CloudinaryShowcase.astro';
import { getImageUrl } from '../lib/cloudinary';
import type { CollectionEntry } from 'astro:content';

interface Props {
  post: CollectionEntry<'blog'>;
}

const { post } = Astro.props;
const { title, description, publishDate, coverImage, coverAlt, tags, author } = post.data;

const date = publishDate.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});

// OG image generated by Cloudinary at build time — no manual export needed
const ogImage = getImageUrl(coverImage, { width: 1200, height: 630, crop: 'fill' });
---

<Layout title={`${title} | AstroBlog`} description={description} ogImage={ogImage}>
  <article>
    <!-- ── Hero image with gradient fade into content ── -->
    <div class="hero">
      <CloudinaryImage
        publicId={coverImage}
        alt={coverAlt}
        width={1440}
        height={560}
        widths={[768, 1024, 1440, 1920]}
        sizes="100vw"
        crop="fill"
        gravity="auto"
        loading="eager"
        class="hero-img"
      />
      <!-- Gradient fades the image into the white background below -->
      <div class="hero-gradient" aria-hidden="true"></div>
    </div>

    <div class="container">
      <!-- ── Post header ── -->
      <header class="post-header">
        <div class="tags">
          {tags.map(tag => <span class="tag">{tag}</span>)}
        </div>
        <h1>{title}</h1>
        <div class="meta">
          <span class="author">{author}</span>
          <span class="dot" aria-hidden="true">·</span>
          <time datetime={publishDate.toISOString()}>{date}</time>
        </div>
      </header>

      <!-- ── Live transformation showcase ── -->
      <CloudinaryShowcase publicId={coverImage} alt={coverAlt} />

      <!-- ── Article prose ── -->
      <div class="prose">
        <slot />
      </div>

      <footer class="post-footer">
        <a href="/blog" class="back">&larr; Back to all posts</a>
      </footer>
    </div>
  </article>
</Layout>

<style>
  /* ── Hero ── */
  .hero {
    position: relative;
    width: 100%;
    height: clamp(300px, 48vw, 560px);
    overflow: hidden;
    background: var(--bg-secondary);
  }

  .hero-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  /* Fades the hero image smoothly into the white page below */
  .hero-gradient {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to bottom,
      transparent 40%,
      rgb(255 255 255 / 0.6) 75%,
      var(--bg) 100%
    );
  }

  /* ── Layout ── */
  .container {
    max-width: var(--max-prose);
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  /* ── Post header ── */
  .post-header {
    padding: 2rem 0 1.75rem;
    border-bottom: 1px solid var(--border);
    margin-bottom: 0;
  }

  .tags {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
    margin-bottom: 1rem;
  }

  .tag {
    font-size: 0.6875rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    padding: 0.2rem 0.6rem;
    border-radius: 9999px;
    background: var(--accent-light);
    color: var(--accent);
  }

  .post-header h1 {
    font-size: clamp(1.75rem, 4vw, 2.75rem);
    letter-spacing: -0.035em;
    line-height: 1.2;
    margin-bottom: 1.25rem;
  }

  .meta {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--text-muted);
    font-size: 0.9375rem;
  }

  .author {
    font-weight: 500;
  }

  .dot {
    opacity: 0.35;
  }

  /* ── Prose ── */
  .prose :global(h2) {
    font-size: 1.625rem;
    margin-top: 3rem;
    margin-bottom: 0.875rem;
    letter-spacing: -0.025em;
  }

  .prose :global(h3) {
    font-size: 1.25rem;
    margin-top: 2.25rem;
    margin-bottom: 0.625rem;
  }

  .prose :global(p) {
    font-size: 1.0625rem;
    line-height: 1.85;
    margin-bottom: 1.5rem;
    color: var(--text);
  }

  .prose :global(a) {
    color: var(--accent);
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: 3px;
  }

  .prose :global(ul),
  .prose :global(ol) {
    padding-left: 1.5rem;
    margin-bottom: 1.5rem;
  }

  .prose :global(li) {
    font-size: 1.0625rem;
    line-height: 1.85;
    margin-bottom: 0.375rem;
  }

  .prose :global(blockquote) {
    border-left: 3px solid var(--accent);
    padding-left: 1.25rem;
    margin: 2rem 0;
    color: var(--text-muted);
    font-style: italic;
  }

  .prose :global(table) {
    width: 100%;
    border-collapse: collapse;
    margin: 2rem 0;
    font-size: 0.9375rem;
  }

  .prose :global(th),
  .prose :global(td) {
    text-align: left;
    padding: 0.625rem 0.875rem;
    border: 1px solid var(--border);
  }

  .prose :global(th) {
    background: var(--bg-secondary);
    font-weight: 600;
  }

  .prose :global(code) {
    font-family: var(--font-mono);
    font-size: 0.875em;
    background: var(--bg-secondary);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    padding: 0.1em 0.35em;
  }

  .prose :global(pre) {
    background: #0f172a;
    color: #e2e8f0;
    border-radius: var(--radius-md);
    padding: 1.5rem;
    overflow-x: auto;
    margin: 2rem 0;
    font-size: 0.875rem;
    line-height: 1.7;
  }

  .prose :global(pre code) {
    background: none;
    border: none;
    padding: 0;
    font-size: inherit;
    color: inherit;
  }

  /* ── Footer ── */
  .post-footer {
    margin-top: 4rem;
    padding: 2rem 0 4rem;
    border-top: 1px solid var(--border);
  }

  .back {
    color: var(--accent);
    font-weight: 500;
    font-size: 0.9375rem;
    text-decoration: none;
  }

  .back:hover {
    text-decoration: underline;
  }
</style>
